// Single source of truth for brand assets (sourced from /assets/images).
// `-light` variants are the dark mark on transparent bg (for light surfaces).
// `-dark` variants are the white mark on transparent bg (for dark surfaces).
import wordmarkOnLight from "./images/lg_logo-light.png";
import wordmarkOnDark from "./images/lg_logo-dark.png";
import monogramOnLight from "./images/logo-light.png";
import monogramOnDark from "./images/logo-dark.png";

export const logos = {
  wordmarkOnLight,
  wordmarkOnDark,
  monogramOnLight,
  monogramOnDark,
};
