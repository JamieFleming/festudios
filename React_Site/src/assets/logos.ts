// Single source of truth for optimized brand assets.
// `-light` variants are the dark mark on transparent bg (for light surfaces).
// `-dark` variants are the white mark on transparent bg (for dark surfaces).
import wordmarkOnLight from "./optimized/lg_logo-light.png";
import wordmarkOnDark from "./optimized/lg_logo-dark.png";
import monogramOnLight from "./optimized/logo-light.png";
import monogramOnDark from "./optimized/logo-dark.png";

export const logos = {
  wordmarkOnLight,
  wordmarkOnDark,
  monogramOnLight,
  monogramOnDark,
};
